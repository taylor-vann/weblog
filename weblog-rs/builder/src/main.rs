// wants
/*
    - generate pages
        - go through list of pages and generate
    - generate blog posts (stretch)
        - chunk through blog posts
        - if no updates skip
        - if new or updated create a new blog post
        - grab te
*/

#[tokio::main]
async fn main() -> Result<(), String> {
    Ok(())
}
